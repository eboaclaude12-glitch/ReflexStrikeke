# ReflexStrike Trading Bot — Python dependencies
# Install with: pip install -r requirements.txt
# Works on macOS, Linux, and Windows (Python 3.11+)

metaapi-cloud-sdk>=27.0.0
pandas>=2.0.0
numpy>=1.24.0
aiohttp>=3.9.0
